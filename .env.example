# Gemini Developer API (AI Studio) — NOT Vertex
GOOGLE_API_KEY=your-key-here

# the live model + Mira's native voice (NOT her aniradio TTS voice — Live has its own set)
LIVE_MODEL=gemini-3.1-flash-live-preview
LIVE_VOICE=Aoede

# the HTTP port
PORT=8000

# set to "minimal" to run the ~30-line primitive instead of the full DJ
# SOCKET_HANDLER=minimal
